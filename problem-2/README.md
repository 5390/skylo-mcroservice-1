### SIM Data Procssing Service 
**To Start Service **
Just Run the main.go

```go run main.go```

DataBase Configurations:
``` postgres://user:password@localhost:5432/retry_db?sslmode=disable ```

Same DB is used for this problem also.

